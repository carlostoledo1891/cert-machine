# hunts

One directory per campaign. `make new-hunt SLUG=<name>` scaffolds one from
`machine/funnel/skeleton/`.

A hunt owns its `target.js`, its `program.md`, its `experiments/` records and its
`best.json` board. The machine writes nowhere else.
