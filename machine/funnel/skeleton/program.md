# program.md — <slug> funnel briefing

> Documentation for the person (or proposer model) working this instance.
> Loaded by NO code — the machine enforces its rules in funnel.js, not here.
> Template per the autoresearch audit's adopted briefing format
> (brain/AUTORESEARCH_AUDIT_2026-08-20.md, program.md row: ADOPT).

## Target

TODO — the mathematical object being searched, in two sentences. Name the open
question it feeds and the brain/literature entries already consulted
(`node brain/brain.js find <term>` BEFORE writing this file).

## Candidate schema

TODO — what a candidate IS, in plain words, mirroring `candidateSchema` in
target.js. State the box: which parameters, which ranges, what the enum order
enumerates.

## Score semantics

TODO — what the float score rewards and WHY that direction is interesting.
State plainly: the score steers, it never admits; it is untrusted until the
score battery passes (knownBad below every planted hit; scaleInflate cannot
raise it).

## Screen semantics

TODO — what the cheap prune keeps and what it may wrongly drop. The reject
audit measures the false-negative rate every run; record here what rate you
expect and what rate would mean the screen is miscalibrated.

## Certificate

TODO — what certify() proves, with which instrument (core/interval enclosure,
BigInt decision, exact rational), and what recheckCertificate recomputes
independently. If certify can REFUSE, state the honest boundary.

## Forbidden moves

- No claim leaves this directory: probe rules bind (CLAUDE.md §Probes).
- Nothing here mints a ledger entry, sends an email, or ships a page.
- Probe numbers that later ship are re-derived inside the gated mission (C4).
- TODO — instance-specific forbidden moves (e.g. "do not edit the kernel",
  "do not touch prepare-side normalization").

## Budget

TODO — pre-authorized decades (default [2] = 100 candidates; authorizing 10^3+
is a decision made HERE, before the run), wall-clock cap, and the probe
time-box line in research/probes/PROBES.md this instance lives under.
