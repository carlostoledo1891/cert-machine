# full-traffic subsets are large local artifacts — regenerable byte-for-byte
# from the PINS.json-pinned release tars via audit/extract.js; only the
# helicopter corpora (*.heli.jsonl) and pins/stats are committed
*.full.jsonl
*.heli.jsonl
day-*/raw/
# raw civil registries (FAA ReleasableAircraft.zip + extracted txt, ANAC
# dados_aeronaves.csv): local, re-fetchable, sha-pinned in
# registry/REGISTRY-PINS.json; only the corpus-joined extracts are committed
registry/raw/
# committed: *.heli.jsonl.gz + PINS.json + extract-stats.json + measure.json
*.certs.jsonl
nyc.pmtiles
*.replay.json
*.ambient.json
